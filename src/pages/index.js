var React = require("react");
var Header = require("../components/Header");

export default () => (
  <div style={{ color: `purple` }}>
    <Header />
    <h1>Hello Gatsby!</h1>
    <p>What a world.</p>
  </div>
);
